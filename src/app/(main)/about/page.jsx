import React from 'react';

export const metadata = {
  title: "Dragon News - About",
};

const AboutPage = () => {
    return (
        <div>
            About page
        </div>
    );
};

export default AboutPage;